akka {
  loggers = ["akka.event.slf4j.Slf4jLogger"]
  loglevel = "INFO"

  default-dispatcher.core-pool-size-min = 2
  default-dispatcher.core-pool-size-max = 2

  default-dispatcher.fork-join-executor.pool-size-min = 2
  default-dispatcher.fork-join-executor.pool-size-max = 2
}

org.nkvoll.gpsd {
    host = "localhost"
    port = 2947

    lookup {
        driverClass = "org.sqlite.JDBC" // "jdbc:sqlite:/Users/njal/Downloads/NO/locations.db"
        //connectionUrl = "jdbc:sqlite:locations.db"
        connectionUrl = "jdbc:sqlite:/Users/njal/Downloads/NO/locations.db"
    }
}